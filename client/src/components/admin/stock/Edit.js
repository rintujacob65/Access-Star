import React from 'react'
import StockForm from './Form'
import { connect } from 'react-redux'

function StockEdit(props) {

    return(
        <div>
            <h2>Edit Stock</h2>
        </div>
    )
}
export default connect()(StockEdit)
